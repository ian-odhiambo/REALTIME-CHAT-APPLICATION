# TODO - Logout fix & UI expectation

- [ ] Fix LogoutButton hook destructuring bug (`logout` vs `logOut`) so logout actually runs.
- [ ] (Optional) Make “NoChatSelected” render after signup if desired (currently `noChatSelected=false`).
- [ ] Run frontend dev server / lint to confirm no runtime errors.

